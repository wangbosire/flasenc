# 管理端 API 根（含协议与主机，无尾斜杠）。留空则浏览器请求同源 `/admin/v1/...`，开发时由 Rsbuild 代理到 Nest（默认 http://localhost:3001）。
# PUBLIC_ADMIN_API_ORIGIN=http://localhost:3001

# 可选：覆盖代理目标（仅开发 server.proxy 使用）
# ADMIN_PROXY_TARGET=http://127.0.0.1:3001
